window.innerWidth < 768 && [].slice.call(document.querySelectorAll("[data-bss-disabled-mobile]")).forEach((function(e) {
    e.classList.remove("animated"),
    e.removeAttribute("data-bss-hover-animate"),
    e.removeAttribute("data-aos"),
    e.removeAttribute("data-bss-parallax-bg"),
    e.removeAttribute("data-bss-scroll-zoom")
}
)),
document.addEventListener("DOMContentLoaded", (function() {
    "AOS"in window && AOS.init()
}
), !1);

    var scene = document.getElementById("scene")
    if(scene){
        var parallaxInstance = new Parallax(scene)
    }
    var scene2 = document.getElementById("scene2");
    if(scene2){
        var parallaxInstance = new Parallax(scene2);
    }
    
    
$((function() {
    $(".navbar li").removeClass("active");
    var e = $("body").attr("id");
    $(".navbar").find("." + e).parent("li").addClass("active"),
    $(".navbar").find("." + e).parents(".dropdown").children(".dropdown-toggle").trigger("click")
}
)),
function() {
    "use strict";
    var e = document.querySelector("#mainNav");
    if (e) {
        var a = function() {
            (void 0 !== window.pageYOffset ? window.pageYOffset : (document.documentElement || document.body.parentNode || document.body).scrollTop) > 100 ? e.classList.add("navbar-shrink") : e.classList.remove("navbar-shrink")
        };
        a(),
        document.addEventListener("scroll", a)
    }
}();
